{
  "name": "buddy",
  "version": "1.0.0",
  "description": "Your personal AI assistant that remembers everything about you",
  "private": true,
  "scripts": {
    "setup": "npm install && cd backend && npm install && cd ../extension && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:extension": "cd extension && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:extension": "cd extension && npm run build",
    "build": "npm run build:backend && npm run build:extension",
    "clean": "rm -rf node_modules backend/node_modules extension/node_modules backend/dist extension/dist"
  },
  "keywords": [
    "ai",
    "assistant",
    "personal",
    "privacy",
    "local",
    "chrome-extension"
  ],
  "author": "Manmit Tiwade",
  "license": "MIT"
}

